package com.arrays;

public class IsUniqueIntegersInArray {
    public static void main(String[] args) {
        int[] numbers = {3, 5, 7, 3};
        System.out.println(isUniqueChars(numbers));
    }

    private static boolean isUniqueChars(int[] numbers) {
        for (int i = 0; i < numbers.length; ++i) {

        }
        return true;

    }
}
